@{ 
    if (Model == null)
    {
        return;
    }

    Subject subject = Model as Subject;
    if (subject == null)
    {
        return;
    }
}

<h3>&nbsp;信用评级(关联)</h3>
@*<table class="table table-bordered">
    <tr>
        <th>信用评级：</th>
        <td><h2 style="margin-top:5px;"><label class="label label-danger">@(subject.Level)</label></h2> </td>
        <th>轻微不诚信</th>
        <td><label class="label label-success">@(subject.Good)</label> </td>
    </tr>
    <tr>
        <th>评级时间：</th>
        <td>@(subject.GradeTime.HasValue ? subject.GradeTime.Value.ToString("yyyy-MM-dd") : "未评级")</td>
        <th>不诚信行为</th>
        <td><label class="label label-warning">@(subject.Less)</label></td>
    </tr>
    <tr>
        <th>信用评级说明：</th>
        <td>@(subject.LevelDescription)</td>
        <th>严重不诚信</th>
        <td><label class="label label-danger">@(subject.Bad)</label> </td>
    </tr>
</table>*@

<table class="table table-striped table-bordered">
    <tr>
        <th>信用评级：</th>
        <td><h2 style="margin-top:5px;"><label class="label label-danger">@(subject.Degree.HasValue?subject.Degree.Value.ToString():"未评审")</label></h2> </td>
        <th>最终分值：</th>
        <td>@(subject.DeDuck)</td>
    </tr>
    <tr>
        <th>购地次数：</th>
        <td>@(subject.Times ?? 0)</td>
        <th>购地记录扣分值：</th>
        <td>@(subject.Values ?? .0)</td>

    </tr>
    <tr>
        <th>购地平均分值：</th>
        <td>@(subject.Average)</td>
        <th>违法用地扣分值：</th>
        <td>@(subject.Record ?? 0)</td>
    </tr>
</table>